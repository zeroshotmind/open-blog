---
# Required fields
title: "Your Post Title"
slug: your-post-slug          # must match the filename (without .mdx) or folder name
date: "2025-01-01"            # ISO 8601 — determines which posts/YYYY/ folder this lives in
summary: "A one or two sentence description of what this post covers. Shown in listings."
tags:
  - inference                 # lowercase, hyphenated
  - llm
authors:
  - name: Your Name
    github: your-github-handle
    # orcid: "0000-0000-0000-0000"   # optional, for scholarly posts
publishOnZeroShotMind: true   # required — omitting this keeps the post off zeroshotmind.com

# Optional fields
# series:
#   name: "Series Name"
#   order: 1                  # position within the series
# draft: true                 # set to exclude from all builds
---

{/* Introduction — set up the problem and why it matters */}

One or two paragraphs introducing the topic. State what the reader will understand by the end.

## Background

Any prerequisite concepts the reader needs. Keep this tight — link out rather than re-explaining basics.

## Main section

The core of the post. Break into subsections as needed.

### Subsection

Use `$...$` for inline math: $\mathbf{Q} = \mathbf{X} W_Q$

Use `$$...$$` for display math:

$$
\text{Attention}(Q, K, V) = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}\right) V
$$

Use fenced code blocks with a language tag:

```python
import torch

def scaled_dot_product_attention(Q, K, V):
    d_k = Q.size(-1)
    scores = torch.matmul(Q, K.transpose(-2, -1)) / d_k ** 0.5
    weights = torch.softmax(scores, dim=-1)
    return torch.matmul(weights, V)
```

{/* If this post has a custom component, import it here: */}
{/* import MyComponent from './MyComponent' */}
{/* Then use it: */}
{/* <MyComponent /> */}

## Key takeaways

- Bullet point summary of the most important ideas
- Keep each point to one sentence

## References

1. Author, A. (Year). *Paper title*. Conference/Journal. [link](https://example.com)
